# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved
*.xcodeproj
*.xcworkspace

# .NET (windows/)
[Bb]in/
[Oo]bj/
*.user
.vs/

# Build output
dist/
# NB: don't use a bare "*.app/" here — on case-insensitive macOS filesystems it
# also matches the Windows "WeChatMulti.App/" project dir. The built macOS
# bundle lives under dist/ (above), which already covers it.

# macOS
.DS_Store
*.swp
*.swo

# Editors
.vscode/
.idea/
*~

# Logs
*.log
/tmp/wcm*.log

# Local screenshot previews of the showcase site (rendered on demand, not deployed)
site/.preview/
